# ─────────────────────────────────────────────────────────────────────────────
# Project Signal — Python Dependencies
# Zero-Cost Stack: Gemini | Supabase | Qdrant | Oracle VM | Vercel
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Intelligence ──────────────────────────────────────────────────────────
google-generativeai>=0.5.0          # Gemini 1.5 Flash API (free tier: 15 RPM)

# ── Web Scraping & OSINT ─────────────────────────────────────────────────────
playwright>=1.42.0                  # Headless browser scraping (Oracle VM)
beautifulsoup4>=4.12.0              # HTML parsing
duckduckgo-search>=6.1.0            # Zero-cost OSINT: ICP search + signal dorks
feedparser>=6.0.11                  # Google Alerts RSS feed parsing
httpx>=0.27.0                       # Async HTTP client

# ── Database & State ─────────────────────────────────────────────────────────
supabase>=2.4.0                     # Supabase Python client (PostgreSQL + Realtime)
psycopg2-binary>=2.9.9              # PostgreSQL driver (for direct DB migrations)

# ── Vector Store & RLHF ──────────────────────────────────────────────────────
qdrant-client>=1.9.0                # Qdrant Cloud vector store
sentence-transformers>=2.7.0        # Local embedding: all-MiniLM-L6-v2 (RLHF)

# ── Email Enrichment ─────────────────────────────────────────────────────────
dnspython>=2.6.1                    # MX record resolution for zero-cost SMTP verification

# ── Data Models & Validation ─────────────────────────────────────────────────
pydantic[email]>=2.7.0              # Pydantic v2 schemas & settings
pydantic-settings>=2.2.0            # BaseSettings for typed env var loading

# ── Scheduling & Utilities ───────────────────────────────────────────────────
schedule>=1.2.1                     # Lightweight cron for Oracle VM workers
python-dotenv>=1.0.0                # .env file loading
pandas>=2.2.0                       # Data manipulation for exports

# ── Code Quality (dev) ────────────────────────────────────────────────────────
ruff>=0.4.0                         # Fast linter (CI)
mypy>=1.10.0                        # Static type checker (CI)
